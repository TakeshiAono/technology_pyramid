<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @technology.name %>
</p>

<p>
  <strong>Public flag:</strong>
  <%= @technology.public_flag %>
</p>

<p>
  <strong>Work:</strong>
  <%= @technology.work_id %>
</p>

<%= link_to 'Edit', edit_technology_path(@technology) %> |
<% if session[:before_controller_path].present? %>
  <%= link_to 'Back', pyramids_path(session[:top_technology_id]) %>
  <%= session[:top_technology_id] = nil %>
<% else %>
  <%= link_to 'Back', technologies_path %>
<% end %>
